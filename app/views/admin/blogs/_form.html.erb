<%= form_with(model: admin_blog) do |form| %>
  <% if admin_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_blog.errors.count, "error") %> prohibited this admin_blog from being saved:</h2>

      <ul>
        <% admin_blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.rich_text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
