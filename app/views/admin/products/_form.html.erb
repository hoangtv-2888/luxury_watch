<% if false %>
<div class="col-lg-7">
  <div class="p-5">
    <%= form_for [:admin, @product], class: "form-inline" do |f| %>
        <%= f.label :images, t("images") %>
        <%= f.file_field :images, multiple: true , class: "mt-5" %>
        <%= f.file_field :images, multiple: true , class: "mt-5" %>
        <%= f.file_field :images, multiple: true , class: "mt-5" %>
        <%= f.label :category_id, t("category"), {class: "required mt-5"} %>
        <br/>
        <%= f.collection_select :category_id, Category.select(:id, :name).order(:name),
                                              :id, :name, {}, {class: "form-control"} %>
        <br/>
        <%= f.label :name, t("name"), {class: "required"} %>
        <%= f.text_field :name, {class: "form-control"} %>
        <%= f.label :desc, t("desc"), {class: "required"} %>
        <%= f.text_area :desc, {class: "form-control"} %>
        <%= f.label :material, t("material")%>
        <%= f.text_field :material, {class: "form-control"} %>
      <div class="form-group">
        <%= f.fields_for :product_detail do |t| %>
          <%= render "product_detail_fields", f: t %>
        <% end %>
      </div>
      <div class="links">
        <%= link_to_add_association t("add"), f, :product_detail, class: "btn btn-primary mt-5" %>
      </div>
      <%= f.submit yield(:button_text), class: "btn btn-primary mt-5" %>
    <% end %>
  </div>
</div>
<% end %>



<div class="content container-fluid">
<div class="container">
<div class="row">
<div class="col-sm-12">
  <!-- general form elements -->
  <div class="box box-primary">
    <h2 class="text-center" >Thêm mới sản phẩm</h2>
    <!-- /.box-header -->
    <!-- form start -->
    <%= form_for [:admin, @product], class: "form-inline" do |f| %>
      <div class="box-body">
        <div class="row">
          <div class="col-sm-3">
            <%= f.label :images, t("images") %>
          </div>
          <div class="col-sm-3">
            <%= f.file_field :images, multiple: true , class: "mt-5" %>
          </div>
          <div class="col-sm-3">
            <%= f.file_field :images, multiple: true , class: "mt-5" %>
          </div>
          <div class="col-sm-3">
            <%= f.file_field :images, multiple: true , class: "mt-5" %>
          </div>
        </div>

        <div class="row">
          <div class="col-sm-6">
            <%= f.label :category_id, t("category"), {class: "required mt-5"} %>
          </div>
          <div class="col-sm-6">
            <%= f.label :name, t("name"), {class: "required"} %>
          </div>
        </div>

        <div class="row">
          <div class="col-sm-6">
            <%= f.collection_select :category_id, Category.select(:id, :name).order(:name),
                                                  :id, :name, {}, {class: "form-control"} %>
          </div>
          <div class="col-sm-6">
            <%= f.text_field :name, {class: "form-control"} %>
          </div>
        </div>
        <%= f.label :desc, t("desc"), {class: "required"} %>
        <%= f.text_area :desc, {class: "form-control"} %>
        <%= f.label :material, t("material")%>
        <%= f.text_field :material, {class: "form-control"} %>
        <div class="form-group">
          <%= f.fields_for :product_detail do |t| %>
            <%= render "product_detail_fields", f: t %>
          <% end %>
        </div>
        <div class="links">
          <%= link_to_add_association t("add"), f, :product_detail, class: "btn btn-primary mt-5" %>
        </div>
      </div>

      <div class="box-footer">
         <%= f.submit yield(:button_text), class: "btn btn-primary mt-5" %>
      </div>
    <% end %>
  </div>
  <!-- /.box -->

  <!-- Form Element sizes -->

</div>
</div>
</div>
</div>